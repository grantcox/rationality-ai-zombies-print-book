.venv/
__pycache__/
*.pyc

# Everything under build/ is derived from the mirror and regenerated by the
# pipeline. Nothing in here is edited by hand and nothing is committed --
# overrides.csv at the repository root is the only durable human input.
build/

# The source mirror (14MB of book text); not ours to redistribute.
www.readthesequences.com/

# covers/coverN.jpg are durable inputs and are committed. What sits beside
# them is working material -- 56MB of intermediate upscales and the mockups
# they were judged from.
covers/upscaling/
covers/mockups/

.DS_Store
